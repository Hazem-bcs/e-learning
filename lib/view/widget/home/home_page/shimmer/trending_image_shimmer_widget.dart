import 'package:flutter/material.dart';

class TrendingImageShimmerWidget extends StatelessWidget {
  const TrendingImageShimmerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      color: Colors.grey.withOpacity(0.5),
    );
  }
}
